const bookControllers = require('./book');
const tagsControllers = require('./tags');
const connectionController = require('./connection');

module.exports = {
  bookControllers,
  tagsControllers,
  connectionController,
};
